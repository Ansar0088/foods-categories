export interface SaleItem {
    name: string;
    price: number;
  }
  
  export interface Category {
    name: string;
    sale_items: SaleItem[];
  }
  export interface Category {
    name: string;
    sale_items: SaleItem[];
  }